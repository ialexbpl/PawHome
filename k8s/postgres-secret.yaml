apiVersion: v1
kind: Secret
metadata:
  name: pawhome-postgres-secret
  namespace: pawhome
type: Opaque
stringData:
  # Password for external PostgreSQL (Docker VM).
  POSTGRES_PASSWORD: your-strong-password
